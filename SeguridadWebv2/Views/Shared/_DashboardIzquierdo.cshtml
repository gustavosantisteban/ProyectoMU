<div id="sidebar" class="nav-collapse">
    <!-- sidebar menu start-->
    <ul class="sidebar-menu" id="nav-accordion">
        <p class="centered"><a href="profile.html"><img src="assets/img/ui-sam.jpg" class="img-circle" width="60"></a></p>
        <h5 class="centered">@User.Identity.Name</h5>

        <li class="sub-menu">
            <a class="active" href="javascript:;">
                <i class="fa fa-dashboard"></i>
                <span>Seguridad</span>
            </a>
            <ul class="sub">
                @if (Request.IsAuthenticated && User.IsInRole("Admin") || User.IsInRole("AllUsuarios"))
                {
                    <li>@Html.ActionLink("Usuarios", "Index", "UsuariosAdmin")</li>
                }
                @if (Request.IsAuthenticated && User.IsInRole("Admin") || User.IsInRole("AllGrupos"))
                {
                    <li>@Html.ActionLink("Grupos", "Index", "GruposAdmin")</li>
                }
                @if (Request.IsAuthenticated && User.IsInRole("Admin") || User.IsInRole("AllPermisos"))
                {
                    <li>@Html.ActionLink("Acciones", "Index", "AccionesAdmin")</li>
                }
            </ul>
        </li>

        <li class="sub-menu">
            <a href="javascript:;">
                <i class="fa fa-suitcase" ></i>
                <span>Vendedores</span>
            </a>
            <ul class="sub">
                @if (Request.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <li>@Html.ActionLink("Listar Vendedores", "Index", "Vendedor")</li>
                }
                @if (Request.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <li>@Html.ActionLink("Reportes", "Reportes", "Vendedor")</li>
                }
            </ul>
        </li>
        <li class="sub-menu">
            <a href="javascript:;">
                <i class=" fa fa-bar-chart-o"></i>
                <span>Organizadores</span>
            </a>
            <ul class="sub">
                @if (Request.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <li>@Html.ActionLink("Listar Organizadores", "Index", "Organizador")</li>
                }
                @if (Request.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <li>@Html.ActionLink("Reportes", "Reportes", "Organizador")</li>
                }
            </ul>
        </li>
        <li class="sub-menu">
            <a href="javascript:;">
                <i class=" fa fa-bar-chart-o"></i>
                <span>Asociados</span>
            </a>
            <ul class="sub">
                @if (Request.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <li>@Html.ActionLink("Listar Asociados", "Index", "Asociados")</li>
                }
                @if (Request.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <li>@Html.ActionLink("Reportes", "Reportes", "Asociados")</li>
                }
            </ul>
        </li>
    </ul>
    <!-- sidebar menu end-->
</div>

